apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catsdogs-api-monitor
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: catsdogs-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
